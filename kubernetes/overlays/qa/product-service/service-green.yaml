apiVersion: v1
kind: Service
metadata:
  name: product-service-green
  namespace: ecommerce-qa
spec:
  selector:
    app: product-service
    version: green
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
